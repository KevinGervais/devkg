import { css } from "styled-components"

export const JoinUsDropdownStyled = css`
  svg {
    height: 20px;
    color: var(--primary-500) !important;
  }
  span {
    display: flex;
    align-items: center;
  }
`